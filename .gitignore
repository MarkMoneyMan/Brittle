.env
__pycache__/
*.pyc
report*.json
brittle-findings.json
py_findings.json
js_findings.json

# pip/setuptools build artifacts from `pip install .` / `pip install -e .`
build/
*.egg-info/

# Local GitHub token for non-interactive push (never committed) — this
# only exists on the device copy, but keeping the entry here too so a
# future cloud->device .gitignore sync doesn't drop it again (it did,
# once — see git history).
.github_token
